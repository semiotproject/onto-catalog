"use strict";

let React = require('react');

export default class ActuatorView extends React.Component {

    constructor(props) {
        super(props);

        // instead of getInitialState in new React notation
        this.state = {

        };
    }

    render() {
        return (
            <div>
                Not implemented
            </div>
        );
    }
}
